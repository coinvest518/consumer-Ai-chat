import { z } from "zod";
import { AIMessage, ToolMessage } from "@langchain/core/messages";
declare const createHandoffTool: ({ agentName }: {
    agentName: string;
}) => import("@langchain/core/tools").DynamicStructuredTool<z.ZodObject<{}, "strip", z.ZodTypeAny, {}, {}>>;
declare function createHandoffBackMessages(agentName: string, supervisorName: string): [AIMessage, ToolMessage];
export { createHandoffTool, createHandoffBackMessages };
